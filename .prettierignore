
# The search evaluation fixture. Generated by `npm run eval:record`, which
# writes it from `scripts/search-eval/corpus.ts`. Formatting it here would put
# it out of step with the generator on the next recording, and the file is
# data rather than something anybody edits by hand.
tests/fixtures/search-eval/

# Playwright writes these. Nothing in them is hand-edited.
playwright-report/
test-results/
blob-report/
